import {describe,expect,it} from 'vitest'
import {isoDateBogota,normalizeHeader,normalizeId,normalizeText,stableUuid} from './import-utils.mjs'
describe('normalización de importación',()=>{it('limpia espacios no separables y conserva texto',()=>expect(normalizeText(' José\u00a0  Pérez ')).toBe('José Pérez'));it('normaliza aliases con acentos',()=>expect(normalizeHeader('  Posición ')).toBe('POSICION'));it('conserva cédulas como texto',()=>{expect(normalizeId('00123')).toBe('00123');expect(normalizeId('123.0')).toBe('123')});it('interpreta fechas calendario en Bogotá',()=>{expect(isoDateBogota('05/08/2026')).toBe('2026-08-05');expect(isoDateBogota(45874)).toMatch(/^2025-/)});it('genera IDs idempotentes UUID',()=>expect(stableUuid('lote')).toMatch(/^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/))})
